Message to send back to SA dev
Copy/paste verbatim:

Cheers for the detailed answers — all four locked into Customer Web's replit.md. Quick recap of what I actioned + one item that needs your confirmation before I touch wallet code.

Actioned immediately (backward-safe):

Added Declined to the booking status enum on the website. Badge shows "Finding another driver" so the passenger sees a constructive message while dispatch reassigns. Polls every 10s so they catch the next status push.
Lowercase + uppercase Cancelled both filter into the Cancelled section. Was already case-insensitive, now explicitly documented.
Assigned ↔ Accepted and OnTrip ↔ Started treated as aliases per your note.
Field duality (status || Status) is on my list for the next backend pass on myrides.ts.
Closed reserved but unused — kept the style in case it lands later.
Dual mount /api/* + /api/passenger/* stays. Future split into publicRouter/passengerRouter noted as non-blocking refactor for Phase B.
Question that's now blocking wallet Slice 2 — walletLedger/{uid}/{txId} vs current passengerWallet/{key}/entries/{entryId}:

Slice 1 shipped before any Firebase Auth on the passenger side, so wallets are currently keyed by passengerKey (e.g. web_motzeoqg_qugcfu), not by Firebase Auth UID. Your Q4 answer mentions walletLedger/{uid}.

Three options I see, ranked by my preference (smallest change first):

Add a passengerIndex/uid/{uid} → {key} resolver. Keep storage at passengerWallet/{key}/entries forever. SA Portal's admin endpoints take a UID, we resolve to key internally, return the data. Zero migration, zero Slice 1 breakage. ✅ My recommendation.
Dual storage: new mobile-app users get walletLedger/{uid}, web-key users stay on passengerWallet/{key}/entries. Two paths, two reconciliation reports. ❌ Reconciliation pain forever.
Migrate everything to walletLedger/{uid} at mobile-app launch. Migration script needed. Web-only users (no UID) would need a synthetic UID or get migrated when they first download the app. ⚠️ Risky, but cleanest end state.
What's your call? Option 1 unblocks the admin endpoints (/api/admin/wallet/balance/:uid, /ledger/:uid, /reconciliation, /adjust) without me touching any existing Slice 1 data. I can ship them as soon as you confirm $BW_ADMIN_KEY is the agreed shared secret and which env vars you want it in on each Replit.

Last thing — I assume BW_ADMIN_KEY is a new secret you'd want me to generate (long random string), or do you have one already?

What I deliberately did NOT do (per the dev-team rule)
❌ Did not touch the wallet code or Firebase paths — flagged for SA confirmation first
❌ Did not implement /api/admin/wallet/* endpoints yet — waiting on wallet schema decision + BW_ADMIN_KEY secret
❌ Did not split the router into public+passenger yet — non-blocking refactor for Phase B
❌ Did not add requireAppAuth middleware — Phase A is still in effect
❌ Did not change any existing status filters to be more aggressive — pure additions
❌ Did not remove /join page (still kept as backward-safe fallback)
When the SA dev replies with the wallet decision + secret, I'll ship the admin endpoints + status field duality in one pass.